# CryptoTax.eu - Environment Variables
# 
# This app runs entirely client-side with no required environment variables.
# All data is stored in the browser's IndexedDB.
# Price data is fetched from CoinGecko's free public API (no key needed).
#
# No .env file is needed to run this project.
# Just run: npm run dev
